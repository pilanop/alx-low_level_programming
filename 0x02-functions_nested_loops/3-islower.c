#include "main.h"

/**
 * _islowerl -  a function that checks for lowercase character.
 *
 * Return: Always 0.
 */

int _islowerl(int c)
{
	if (c > 96 && c < 123)
	{
		_putchar('0');
	}
	else
	{
		_putchar('1');
	}
}
